import { ref } from 'vue'

const mainShow = {
    announcement: ref(false),
    settings: ref(false),
    about: ref(false),
    screenshotHelper: ref(false),
    savefile: ref(false)
}

export {
    mainShow
}
